var expect = require('chai').expect;

describe('dummy', () => {
    it('should not do anything interesting', () => {
        console.log('running test');
        expect(true).to.equal(true);
    });
});
